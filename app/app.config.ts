export default defineAppConfig({
    ui: {
        colors: {
            primary: 'pancho',
        },
        button: {
            slots: {
                base: 'cursor-pointer',
            },
        },
    },
});
